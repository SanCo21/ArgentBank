import React from 'react';
import Logo from './Logo';
import Nav from './Nav';
 
const Header = () => (
    <nav className="main-nav">
      <Logo />
      <Nav />
    </nav>
  );

export default Header
